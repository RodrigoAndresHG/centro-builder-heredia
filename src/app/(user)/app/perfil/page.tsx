import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function PerfilPage() {
  return (
    <PlaceholderPage
      eyebrow="Usuario"
      title="Perfil"
      description="Base de perfil del usuario, sin autenticacion real todavia."
    />
  );
}
