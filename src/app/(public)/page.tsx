import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function HomePage() {
  return (
    <PlaceholderPage
      eyebrow="Publico"
      title="Centro Builder HeredIA"
      description="Base publica del MVP preparada para la siguiente vertical slice."
      details={["Home publica", "CTA futuro", "Contenido comercial pendiente"]}
    />
  );
}
